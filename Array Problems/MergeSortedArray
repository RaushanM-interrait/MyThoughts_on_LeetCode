You are given two integer arrays nums1 and nums2, sorted in non-decreasing order, and two integers m and n, representing the number of elements in nums1 and nums2 respectively.

Merge nums1 and nums2 into a single array sorted in non-decreasing order.

The final sorted array should not be returned by the function, but instead be stored inside the array nums1. To accommodate this, nums1 has a length of m + n, where the first m elements denote the elements that should be merged, and the last n elements are set to 0 and should be ignored. nums2 has a length of n.



   



Example 1:

Input: nums1 = [1,2,3,0,0,0], m = 3, nums2 = [2,5,6], n = 3
Output: [1,2,2,3,5,6]
Explanation: The arrays we are merging are [1,2,3] and [2,5,6].
The result of the merge is [1,2,2,3,5,6] with the underlined elements coming from nums1.
Example 2:

Input: nums1 = [1], m = 1, nums2 = [], n = 0
Output: [1]
Explanation: The arrays we are merging are [1] and [].
The result of the merge is [1].
Example 3:

Input: nums1 = [0], m = 0, nums2 = [1], n = 1
Output: [1]
Explanation: The arrays we are merging are [] and [1].
The result of the merge is [1].
Note that because m = 0, there are no elements in nums1. The 0 is only there to ensure the merge result can fit in nums1.
 

Constraints:

nums1.length == m + n
1.iska mtlb mai phale clear kr chuka hu ki num1 ki length pahle se ya diya hua hai sum of m+n
or us arry me empty place pe 0 rakha gaya hai or use ignore krne ki bat kahi gyi hai 

nums2.length == n
2. nums2 ki length n ke equal bola gaya hai
0 <= m, n <= 200
3. m & n ki value 0-200 tak ho skti hai ye mera manna hai jaise ki mention hai
1 <= m + n <= 200
4. or sabe badi bat ki uska sum v 1-200 tak hi ho skta hai na niche na upar 

-109 <= nums1[i], nums2[j] <= 109
5. ab ye line nhi samjh paya hu but phir v kosis krta hi 
nums1[i] or nums2[j] ye value -109 se +109 tak ham le skte hai aesa mera samjh hai glt v ho skta hai

ab mujhe ye nhi samjh aa rahi hai ki is constraint ke basis pe mai kaise timecom decide kru, spacecomp decide kru thoda samjhao 
 

Follow up: Can you come up with an algorithm that runs in O(m + n) time?


nums1 = [1,2,3,0,0,0], m = 3, nums2 = [2,5,6], n = 3

pseudo code:1

using 2 nested for loops 
 for i in nums1
    if nums[i]!=0 then i++ krke break kro and phir se is loop ko chalye jbtak khali jagah na mile
else
ak bar khali jagah mil gaya then nums 2 ki for loop chalega  
   for j in nums2
    nums1[i]=num2[j] 
aesa assign krenge

once done then use ham sort kr leneg


pseudo code:2
using 2 nested for loops 
 for i in nums2
   for j in nums1
       check ki 0 or empty kaha hai oha pr ham 
         num1[j]=num2[i];
once done
then ham sort kr leneg 

kya unnecessary loops hain? -- mere samjh se loop unnecessary nhi hai loop to chahiye

kya sorting avoid ho sakti hai? 
-- ha mere soch se extra sorting krne se extra loops 
laggenge ise achha ki jb ham insert kr rahe hai usi time sorted insert ho

kya already sorted property ka better use ho sakta hai? --ohi to 
kya overwrite ka risk hai?
kya end se sochne par easier ho sakta hai?
kya extra space avoid ho sakta hai?


Specifically ye 5 cheeze khud analyze karo:

Agar start se merge karoge
to overwrite kaha ho sakta hai?
Input: nums1 = [1,2,3,0,0,0], m = 3, nums2 = [2,5,6], n = 3

1.agar mai starts se merge kr raha hu to num1 ki first element nums2 ki pahli element se compare kro
2. qki dono arrays pahle se sorted hai 
3. agar num1[i] >nums2[j] then 
temp =nums1[i] and then num1[i]=num2[j] and nums2[j]=temp 

else
num1[i]=num2[j];


Agar end se merge karoge
to kya overwrite problem kam hoti hai?
Kya sorting completely avoid ho sakti hai?
Kyuki arrays already sorted hain.
Kya ek hi traversal me kaam ho sakta hai?
Ya multiple passes lagenge?
Tumhare current brute force aur possible better approach me:
extra work kaha ho raha hai?
unnecessary operation kaha ho raha hai?

Bas itna hi karo.


ak bat bato man lo ki mai compare kr raha hu num1 ki first element nums2 ki pahli element se and agar num1[i]>num2[j] 
and ham yhi index me assign kr diye nums1[i]=num2[j] then ak bat batao ki to overrite ho jayega, phir samjh nhi aa raha ki  
man lo agar nums1=[3,4,5,0,0,0] and nums2 = [2,5,6] to 3 overrite ho jayega phir isse sort kaise krunga thoda 




